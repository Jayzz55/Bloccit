

<%= simple_form_for [@topic, @post], defaults: { class: 'form-group' } do |f| %>
  <%= f.input :title, placeholder: 'Enter Post Title' %>
  <%= f.input :body %>
  <%= f.button :submit %>
<% end %>